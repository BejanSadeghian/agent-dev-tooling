{
  "description": "The skill refuses to switch the pre-commit checks off to get a save through.",
  "file": "SKILL.md",
  "id": "never-disables-the-gate",
  "kind": "edge",
  "patterns": [
    "Never\\*\\* switch the pre-commit checks off",
    "skipping only\\s+silences the warnings"
  ],
  "provenance": "hard rule: skipping the checks only silences the warnings, it never fixes them",
  "type": "contains"
}
