{
  "id": "gate-commands-in-order",
  "description": "The check step runs the single check command and publishes with the checks confirmed.",
  "type": "contains",
  "kind": "accuracy",
  "file": "SKILL.md",
  "patterns": [
    "npm run check",
    "npm run publish",
    "\\.framework/state/<skill>\\.json"
  ],
  "provenance": "hard rule: the recorded run is the evidence the suite ran after the last edit"
}
