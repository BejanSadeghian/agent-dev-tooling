---
description: Run every check and explain any failure in plain language.
---

Run `npm run check`.

If it fails, do not just paste the output. For each failure tell me:

- which skill,
- what it means in plain words,
- the one command that fixes it,

then fix what you can safely fix (re-running a skill's tests, filling a missing test, correcting a
format problem) and run it again. Never switch a check off, and never use `SKIP_SKILL_GATE`.
